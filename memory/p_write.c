/*
 *
 * FUNCTION:
 * p_write(): copies an array from a local pointer to a global address
 *
 * ARGUMENTS:
 * dst      - complete address destination pointer
 * loc_src  - complete address source pointer
 * nb       - number of bytes to copy
 * flags    - independent flags to assert, separate with '|'
 *            ASYNC : Makes copy call asynchronous (ie non-blocking)  
 *
 */
#include <stddef.h>
void *p_copy (void *dst, const void *loc_src, size_t nb, int flags){

    /*PLACE CODE HERE*/

}
